# Drupal state is stored in the Compose project-scoped `drupal_site` volume.
# `docker compose down -v` removes it after every run, so the harness creates
# no generated Drupal directory in this checkout.
